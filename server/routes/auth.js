import express from 'express';
import validator from 'validator';

const router = express.Router();

function validateSignupForm(res) {
  const errors = {};
  const isFormValid = true;
  const message = '';
}
